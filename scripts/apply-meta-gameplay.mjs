import fs from 'node:fs/promises';

const path='index.html';
let html=await fs.readFile(path,'utf8');
const marker='/* Shell Runner meta gameplay v2.0 */';
if(html.includes(marker)){console.log('Meta gameplay expansion already present.');process.exit(0)}

const introAnchor='<p>Wische links oder rechts und laufe durch das Tor mit der richtigen Übersetzung. Nach 3 richtigen Wörtern wartet ein Themen-Boss.</p><button id="introStart">Abenteuer starten</button>';
const introReplacement='<p>Wische links oder rechts und laufe durch das Tor mit der richtigen Übersetzung. Nach 3 richtigen Wörtern wartet ein Themen-Boss.</p><div class="helper-select" id="helperSelect" aria-label="Helfer wählen"><button class="helper-card selected" type="button" data-helper="lumi"><b>✨ Lumi</b><small>zeigt manchmal einen sanften Hinweis</small></button><button class="helper-card" type="button" data-helper="milo"><b>🛡 Milo</b><small>schützt einmal vor einem Herzverlust</small></button><button class="helper-card" type="button" data-helper="nera"><b>🐚 Nera</b><small>bringt zusätzliche Muscheln</small></button></div><button id="introStart">Abenteuer starten</button>';
if(!html.includes(introAnchor))throw new Error('Intro anchor missing');
html=html.replace(introAnchor,introReplacement);

const promptAnchor='<div class="prompt" id="prompt">Bereit?</div>';
const promptReplacement='<div class="prompt" id="prompt">Bereit?</div><div class="run-status" id="runStatus"><span id="comboTier">STARTKLAR</span><span id="helperBadge">✨ Lumi</span><span class="fever-badge" id="feverBadge">🔥 TULA-FIEBER</span></div>';
if(!html.includes(promptAnchor))throw new Error('Prompt anchor missing');
html=html.replace(promptAnchor,promptReplacement);

const css=String.raw`
${marker}
.run-status{position:absolute;z-index:21;left:50%;top:16%;transform:translateX(-50%);display:flex;align-items:center;justify-content:center;gap:6px;width:min(92%,430px);pointer-events:none}.run-status span{padding:5px 8px;border:1px solid rgba(239,200,103,.34);border-radius:999px;background:rgba(2,31,53,.82);color:#dff8f2;font-size:.58rem;font-weight:1000;letter-spacing:.06em;box-shadow:0 5px 12px rgba(0,10,20,.18)}.run-status .fever-badge{display:none;color:#082b44;background:linear-gradient(135deg,#fff1a9,#efc867);border-color:#fff4c2}.app.fever-mode .fever-badge{display:inline-flex;animation:feverBadgePulse .7s ease-in-out infinite alternate}.app.fever-mode .road{filter:saturate(1.16) brightness(1.06);box-shadow:inset 0 0 42px rgba(239,200,103,.12)}.app.fever-mode .runner{filter:drop-shadow(0 0 20px rgba(255,226,126,.45))}.app.fever-mode .progress i{background:linear-gradient(90deg,#ffe18a,#79d9c7,#fff3d0)}
@keyframes feverBadgePulse{to{transform:scale(1.05);box-shadow:0 0 18px rgba(255,225,126,.46)}}
.helper-select{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin:12px 0 14px}.helper-card{min-height:78px!important;padding:8px 6px!important;border-radius:16px!important;background:linear-gradient(180deg,rgba(6,60,91,.95),rgba(2,34,57,.96))!important;color:#effff9!important;box-shadow:none!important;border:1px solid rgba(121,217,199,.28)!important}.helper-card b{display:block;color:#ffe8a6;font-size:.8rem}.helper-card small{display:block;margin-top:4px;color:#ccefe8;font-size:.61rem;line-height:1.22;font-weight:700}.helper-card.selected{border-color:#ffe18a!important;box-shadow:0 0 0 2px rgba(239,200,103,.14),0 8px 18px rgba(0,10,20,.22)!important;transform:translateY(-2px)}
.gate.risk-gate{position:relative;border-color:#fff0a8!important;box-shadow:0 7px 0 #805514,0 0 24px rgba(255,224,118,.45),0 14px 22px rgba(0,10,20,.26)!important;background:linear-gradient(180deg,rgba(105,77,19,.94),rgba(51,39,12,.96))!important}.gate.risk-gate::before{content:"GOLDTOR · x2";position:absolute;left:50%;top:-13px;transform:translateX(-50%);padding:3px 7px;border-radius:999px;background:#ffe18a;color:#082b44;font:1000 .48rem/1 Inter,system-ui,sans-serif;white-space:nowrap;box-shadow:0 3px 9px rgba(0,0,0,.25)}.gate.revenge-gate{border-color:#9ee9dd!important}.gate.helper-hint{animation:helperHintPulse .55s ease-in-out 2 alternate!important}@keyframes helperHintPulse{to{box-shadow:0 7px 0 rgba(60,75,16,.72),0 0 30px rgba(121,217,199,.62)}}
.mastery-pop{position:absolute;z-index:34;left:50%;bottom:34%;transform:translateX(-50%);padding:7px 11px;border-radius:999px;border:1px solid rgba(255,231,160,.7);background:rgba(2,31,53,.96);color:#fff0b6;font-size:.68rem;font-weight:1000;opacity:0;pointer-events:none}.mastery-pop.show{animation:masteryPop .9s ease}@keyframes masteryPop{0%{opacity:0;transform:translate(-50%,8px) scale(.9)}18%,70%{opacity:1;transform:translate(-50%,0) scale(1)}100%{opacity:0;transform:translate(-50%,-14px) scale(.96)}}
@media(max-width:520px){.run-status{top:15.4%;gap:4px}.run-status span{font-size:.51rem;padding:4px 6px}.helper-card{min-height:72px!important;padding:7px 4px!important}.helper-card b{font-size:.72rem}.helper-card small{font-size:.56rem}.gate.risk-gate::before{font-size:.44rem}}
@media(prefers-reduced-motion:reduce){.app.fever-mode .fever-badge,.gate.helper-hint,.mastery-pop.show{animation-duration:.18s!important}}
`;
html=html.replace('</style>',css+'\n</style>');

const arenaEnd='<div class="impact-layer" id="impactLayer" aria-hidden="true"></div></section>';
if(!html.includes(arenaEnd))throw new Error('Arena end anchor missing');
html=html.replace(arenaEnd,'<div class="impact-layer" id="impactLayer" aria-hidden="true"></div><div class="mastery-pop" id="masteryPop">Wort gemeistert!</div></section>');

const stateAnchor='let s,q,timer,touchX,bossTheme,bossRound=0;';
const stateExpansion=`let s,q,timer,touchX,bossTheme,bossRound=0;\nlet selectedHelper='lumi';\nconst MASTERY_LABELS=['Neu','Gesehen','Geübt','Sicher','Gemeistert'];\nfunction loadMastery(){try{return JSON.parse(localStorage.getItem('shellRunnerMasteryV1')||'{}')||{}}catch{return{}}}\nfunction saveMastery(){try{localStorage.setItem('shellRunnerMasteryV1',JSON.stringify(s?.mastery||{}))}catch{}}\nfunction masteryKey(word){return word?.de||''}\nfunction updateMastery(word,correct){if(!word)return 0;const key=masteryKey(word),before=Number(s.mastery[key]||0),after=Math.max(0,Math.min(4,before+(correct?1:-1)));s.mastery[key]=after;saveMastery();if(correct&&after===4&&before<4){const p=$('masteryPop');if(p){p.textContent='⭐ '+word.de+' gemeistert!';p.classList.remove('show');void p.offsetWidth;p.classList.add('show')}}return after}\nfunction comboTierName(combo){if(combo>=12)return'INSEL-LEGENDE';if(combo>=8)return'GOLDENE WELLE';if(combo>=5)return'TULA-FIEBER';if(combo>=3)return'MUSCHEL-SERIE';return combo>0?'COMBO x'+combo:'STARTKLAR'}\nfunction feverActive(){return !!s&&s.feverUntil>Date.now()}\nfunction startFever(){if(!s||feverActive())return;s.feverUntil=Date.now()+10000;document.getElementById('app').classList.add('fever-mode');feedback('🔥 TULA-FIEBER · 10 Sekunden x2 Punkte!');vibrate([28,24,42])}\nfunction syncFever(){const active=feverActive();document.getElementById('app').classList.toggle('fever-mode',active);return active}\nfunction queueRevenge(word){if(!word)return;const key=masteryKey(word);if(!s.revenge.some(r=>r.key===key))s.revenge.push({key,wait:2})}\nfunction takeRevenge(){s.revenge.forEach(r=>r.wait--);const idx=s.revenge.findIndex(r=>r.wait<=0);if(idx<0)return null;const item=s.revenge.splice(idx,1)[0];return words.find(w=>masteryKey(w)===item.key)||null}\nfunction helperName(){return s?.helper==='milo'?'🛡 Milo':s?.helper==='nera'?'🐚 Nera':'✨ Lumi'}\nfunction routeSpeedFactor(){return s?.route==='calm'?.92:s?.route==='adventure'?1.08:s?.route==='treasure'?1.03:1}\nfunction routeRewardFactor(){return s?.route==='adventure'?1.25:s?.route==='treasure'?1.15:1}\nfunction perkActive(id){return Array.isArray(s?.perks)&&s.perks.includes(id)}\n`;
if(!html.includes(stateAnchor))throw new Error('State anchor missing');
html=html.replace(stateAnchor,stateExpansion);

const resetOld="function reset(){clearTimeout(timer);stopBossObjects();s={run:false,paused:false,lane:1,score:0,combo:0,best:0,lives:3,speed:1,correct:0,total:0,answered:0,bossCorrect:0,boss:false,bossMiss:0,shells:24,xp:120,pauseLeft:2};q=null;bossRound=0;$('gates').innerHTML='';$('prompt').textContent='Bereit?';$('runner').style.left='50%';$('bossSide').classList.remove('show');render();updatePause();}";
const resetNew="function reset(){clearTimeout(timer);stopBossObjects();s={run:false,paused:false,lane:1,score:0,combo:0,best:0,lives:3,speed:1,correct:0,total:0,answered:0,bossCorrect:0,boss:false,bossMiss:0,shells:24,xp:120,pauseLeft:2,helper:selectedHelper,helperUsed:false,feverUntil:0,mastery:loadMastery(),revenge:[],sequence:0,perks:[],route:'calm',shieldCharges:0,comboShield:0,timeBoostUntil:0};q=null;bossRound=0;$('gates').innerHTML='';$('prompt').textContent='Bereit?';$('runner').style.left='50%';$('bossSide').classList.remove('show');document.getElementById('app').classList.remove('fever-mode');render();updatePause();}";
if(!html.includes(resetOld))throw new Error('Reset anchor missing');html=html.replace(resetOld,resetNew);

const renderOld="function render(){$('score').textContent=s.score.toLocaleString('de-DE');$('combo').textContent='x'+s.combo;$('speed').textContent=s.speed.toFixed(1)+'×';$('lives').textContent=s.boss?'⚔ '+Math.max(0,3-s.bossMiss)+'/3':'❤'.repeat(s.lives)+'♡'.repeat(3-s.lives);$('shells').textContent=s.shells;$('xp').textContent=s.xp;$('progress').style.width=(s.boss?(bossRound/5)*100:Math.min(100,(s.bossCorrect/BOSS_CORRECT_INTERVAL)*100))+'%';}";
const renderNew="function render(){$('score').textContent=s.score.toLocaleString('de-DE');$('combo').textContent='x'+s.combo;$('speed').textContent=s.speed.toFixed(1)+'×';$('lives').textContent=s.boss?'⚔ '+Math.max(0,3-s.bossMiss)+'/3':'❤'.repeat(s.lives)+'♡'.repeat(3-s.lives);$('shells').textContent=s.shells;$('xp').textContent=s.xp;$('progress').style.width=(s.boss?(bossRound/5)*100:Math.min(100,(s.bossCorrect/BOSS_CORRECT_INTERVAL)*100))+'%';if($('comboTier'))$('comboTier').textContent=comboTierName(s.combo);if($('helperBadge'))$('helperBadge').textContent=helperName();syncFever();}";
if(!html.includes(renderOld))throw new Error('Render anchor missing');html=html.replace(renderOld,renderNew);

const buildOld="function buildNormal(){const target=words[Math.floor(Math.random()*words.length)],dec=shuffle(words.filter(w=>w!==target)).slice(0,2),ans=shuffle([target,...dec]);return{type:'normal',target,answers:ans,correct:ans.indexOf(target)}}";
const buildNew="function buildNormal(){const revengeTarget=takeRevenge(),target=revengeTarget||words[Math.floor(Math.random()*words.length)],dec=shuffle(words.filter(w=>w!==target)).slice(0,2),ans=shuffle([target,...dec]);return{type:'normal',target,answers:ans,correct:ans.indexOf(target),isRevenge:!!revengeTarget,risk:false}}";
if(!html.includes(buildOld))throw new Error('buildNormal anchor missing');html=html.replace(buildOld,buildNew);

const spawnOld="function spawn(){if(!s.run)return;q=s.boss?buildBoss():buildNormal();$('prompt').textContent=s.boss?'Thema: '+bossTheme:q.target.de;const g=$('gates');g.innerHTML='';g.className='gates';const dur=Math.max(1.5,3-(s.speed-1)*.45);g.style.setProperty('--dur',dur+'s');g.style.setProperty('--travel',Math.max(240,$('runner').getBoundingClientRect().top-g.getBoundingClientRect().top-120)+'px');q.answers.forEach((w,i)=>{const b=document.createElement('button');b.className='gate'+(i===s.lane?' sel':'');b.textContent=w.en;b.onclick=()=>move(i);g.appendChild(b)});void g.offsetWidth;g.classList.add('active');timer=setTimeout(resolve,dur*1000+80)}";
const spawnNew="function spawn(){if(!s.run)return;s.sequence++;q=s.boss?buildBoss():buildNormal();if(!s.boss&&!q.isRevenge&&s.combo>=2&&Math.random()<.22)q.risk=true;$('prompt').textContent=s.boss?'Thema: '+bossTheme:(q.isRevenge?'Revanche: ':'')+q.target.de;const g=$('gates');g.innerHTML='';g.className='gates';let dur=Math.max(1.5,3-(s.speed-1)*.45);dur/=routeSpeedFactor();if(s.timeBoostUntil>Date.now()||perkActive('zeitmuschel'))dur*=1.15;if(q.risk)dur*=.86;g.style.setProperty('--dur',dur+'s');g.style.setProperty('--travel',Math.max(240,$('runner').getBoundingClientRect().top-g.getBoundingClientRect().top-120)+'px');q.answers.forEach((w,i)=>{const b=document.createElement('button');b.className='gate'+(i===s.lane?' sel':'')+(q.risk&&i===q.correct?' risk-gate':'')+(q.isRevenge&&i===q.correct?' revenge-gate':'');b.textContent=w.en;b.onclick=()=>move(i);g.appendChild(b)});void g.offsetWidth;g.classList.add('active');const expected=q;if(s.helper==='lumi'&&!s.boss&&s.sequence%4===0)setTimeout(()=>{if(q===expected&&s.run)g.children[q.correct]?.classList.add('helper-hint')},Math.min(1200,dur*450));if(perkActive('piratenblick')&&!s.boss)setTimeout(()=>{if(q===expected&&s.run){const wrong=[...g.children].filter((_,i)=>i!==q.correct);wrong[0]?.classList.add('wrong')}},Math.min(1250,dur*480));timer=setTimeout(resolve,dur*1000+80)}";
if(!html.includes(spawnOld))throw new Error('spawn anchor missing');html=html.replace(spawnOld,spawnNew);

const resolveOld="function resolve(){if(!s.run||!q)return;const correct=s.lane===q.correct,gs=[...document.querySelectorAll('.gate')],selected=gs[s.lane],correctGate=gs[q.correct];gs.forEach((g,i)=>g.classList.add(i===q.correct?'correct':'wrong'));playHitFx(correct,selected,correctGate);playCharacterReactions(correct);if(correct){s.correct++;s.combo++;s.best=Math.max(s.best,s.combo);s.score+=100+s.combo*15;s.shells++;s.xp+=2;$('runner').classList.add('happy');feedback('✓ Richtig!')}else{s.combo=0;if(q.type==='boss')s.bossMiss++;else s.lives--;$('runner').classList.add('sad');feedback(q.type==='boss'?'✕ Nicht passend':'✕ '+q.target.en)}s.total++;if(q.type==='normal'){s.answered++;if(correct)s.bossCorrect++}else bossRound++;render();setTimeout(()=>{clearHitFx();$('runner').classList.remove('happy','sad');s.lane=1;$('runner').style.left='50%';if(s.lives<=0){gameover();return}if(s.boss&&(bossRound>=5||s.bossMiss>=3)){endBoss();return}if(!s.boss&&s.bossCorrect>=BOSS_CORRECT_INTERVAL){showBossIntro();return}spawn()},750)}";
const resolveNew="function resolve(){if(!s.run||!q)return;const correct=s.lane===q.correct,gs=[...document.querySelectorAll('.gate')],selected=gs[s.lane],correctGate=gs[q.correct];gs.forEach((g,i)=>g.classList.add(i===q.correct?'correct':'wrong'));playHitFx(correct,selected,correctGate);playCharacterReactions(correct);const normal=q.type==='normal';if(correct){s.correct++;s.combo++;if(s.combo>=5&&!feverActive())startFever();s.best=Math.max(s.best,s.combo);let points=Math.round((100+s.combo*15)*routeRewardFactor()*(feverActive()?2:1));let shellGain=1+(s.helper==='nera'?1:0);if(q.risk){points+=200;shellGain+=2}if(q.isRevenge){points+=80;s.combo=Math.max(s.combo,2);s.xp+=3}if(perkActive('doppelter-schatz')&&q.risk)shellGain+=1;s.score+=points;s.shells+=shellGain;s.xp+=2;updateMastery(q.target,true);$('runner').classList.add('happy');feedback(q.isRevenge?'✓ Revanche geschafft! +'+points:q.risk?'★ GOLDTOR! +'+points:'✓ Richtig! +'+points)}else{if(normal)queueRevenge(q.target);updateMastery(q.target,false);if(perkActive('kombokette'))s.combo=Math.floor(s.combo/2);else if(s.comboShield>0){s.comboShield--;feedback('🔗 Komboschutz!')}else s.combo=0;if(q.type==='boss')s.bossMiss++;else if(s.helper==='milo'&&!s.helperUsed){s.helperUsed=true;feedback('🛡 Milo schützt dein Herz!')}else s.lives--;$('runner').classList.add('sad');if(q.type==='boss')feedback('✕ Nicht passend');else if(!(s.helper==='milo'&&s.helperUsed))feedback('✕ '+q.target.en)}s.total++;if(normal){s.answered++;if(correct)s.bossCorrect++}else bossRound++;render();setTimeout(()=>{clearHitFx();$('runner').classList.remove('happy','sad');s.lane=1;$('runner').style.left='50%';if(s.lives<=0){gameover();return}if(s.boss&&(bossRound>=5||s.bossMiss>=3)){endBoss();return}if(!s.boss&&s.bossCorrect>=BOSS_CORRECT_INTERVAL){showBossIntro();return}spawn()},750)}";
if(!html.includes(resolveOld))throw new Error('resolve anchor missing');html=html.replace(resolveOld,resolveNew);

const eventsOld="$('introStart').onclick=start;$('pause').onclick=pauseToggle;$('bossStart').onclick=startBoss;$('bossContinue').onclick=continueAfterBoss;$('restart').onclick=start;";
const eventsNew="document.querySelectorAll('.helper-card').forEach(btn=>btn.addEventListener('click',()=>{selectedHelper=btn.dataset.helper||'lumi';document.querySelectorAll('.helper-card').forEach(b=>b.classList.toggle('selected',b===btn))}));$('introStart').onclick=start;$('pause').onclick=pauseToggle;$('bossStart').onclick=startBoss;$('bossContinue').onclick=continueAfterBoss;$('restart').onclick=start;";
if(!html.includes(eventsOld))throw new Error('Event anchor missing');html=html.replace(eventsOld,eventsNew);

await fs.writeFile(path,html,'utf8');
console.log('Applied combo tiers, Tula Fever, revenge/mastery, risk gates and helpers.');
