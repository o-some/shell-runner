import fs from 'node:fs/promises';

const path='index.html';
let html=await fs.readFile(path,'utf8');
const marker='/* Shell Runner unique boss mechanics v2.1 */';
if(html.includes(marker)){console.log('Unique boss mechanics already present.');process.exit(0)}

const statusAnchor='<div class="run-status" id="runStatus"><span id="comboTier">STARTKLAR</span><span id="helperBadge">✨ Lumi</span><span class="fever-badge" id="feverBadge">🔥 TULA-FIEBER</span></div>';
if(!html.includes(statusAnchor))throw new Error('Run status anchor missing');
html=html.replace(statusAnchor,statusAnchor+'<div class="boss-mechanic-banner" id="bossMechanicBanner" aria-live="polite"></div>');

const css=String.raw`
${marker}
.boss-mechanic-banner{position:absolute;z-index:30;left:50%;top:20%;transform:translateX(-50%) translateY(-8px);min-width:170px;max-width:82%;padding:7px 11px;border:1px solid rgba(255,224,145,.48);border-radius:999px;background:rgba(2,25,44,.94);color:#fff0b7;text-align:center;font-size:.64rem;font-weight:1000;letter-spacing:.04em;opacity:0;pointer-events:none;box-shadow:0 8px 20px rgba(0,8,18,.28);transition:.18s}.boss-mechanic-banner.show{opacity:1;transform:translateX(-50%) translateY(0)}
.arena.boss-shadow-phase::after{content:"";position:absolute;z-index:10;inset:0;background:radial-gradient(circle at 50% 70%,transparent 24%,rgba(18,4,40,.28) 64%,rgba(7,1,18,.52));pointer-events:none}.arena.boss-shadow-phase .prompt,.arena.boss-shadow-phase .gate{z-index:13}.gate.shadow-gate{filter:drop-shadow(8px 0 0 rgba(87,48,136,.34)) drop-shadow(-8px 0 0 rgba(45,22,78,.28));border-color:rgba(190,145,255,.62)!important}.gate.boss-swap{animation:bossSwapFlash .52s ease!important}.gate.boss-hook-warning{box-shadow:0 7px 0 rgba(83,52,14,.65),0 0 26px rgba(255,89,85,.38)!important}.gate.boss-wind-warning{box-shadow:0 7px 0 rgba(83,52,14,.65),0 0 28px rgba(121,217,199,.42)!important}@keyframes bossSwapFlash{0%,100%{transform:translateX(0)}45%{transform:translateX(var(--swap-x,0)) scale(.95);filter:brightness(1.22)}}
.boss-object.gold-shell::after,.boss-object.shield-shell::after,.boss-object.time-shell::after,.boss-object.combo-shell::after,.boss-object.thief-shell::after,.boss-object.roll-barrel::after,.boss-object.double-barrel::after,.boss-object.dud-barrel::after,.boss-object.king-barrel::after{position:absolute;z-index:5;right:-5px;top:-5px;display:grid;place-items:center;width:25px;height:25px;border-radius:50%;font-size:.82rem;background:rgba(2,31,53,.92);border:1px solid rgba(255,230,154,.55);box-shadow:0 4px 10px rgba(0,8,18,.3)}.boss-object.gold-shell::after{content:"×3";color:#ffe18a;font-size:.58rem;font-weight:1000}.boss-object.shield-shell::after{content:"🛡"}.boss-object.time-shell::after{content:"⏳"}.boss-object.combo-shell::after{content:"🔗"}.boss-object.thief-shell::after{content:"💰"}.boss-object.roll-barrel::after{content:"↔"}.boss-object.double-barrel::after{content:"×2";color:#ffb6a3;font-size:.58rem;font-weight:1000}.boss-object.dud-barrel::after{content:"?";color:#ffcf8d;font-weight:1000}.boss-object.king-barrel::after{content:"♛";color:#ffe18a}.boss-object.gold-shell::before,.boss-object.shield-shell::before,.boss-object.time-shell::before,.boss-object.combo-shell::before,.boss-object.thief-shell::before{background:radial-gradient(circle,rgba(255,232,139,.88),rgba(239,200,103,.35) 46%,transparent 74%)!important}.boss-object.roll-barrel::before,.boss-object.double-barrel::before,.boss-object.dud-barrel::before,.boss-object.king-barrel::before{background:radial-gradient(circle,rgba(255,72,72,.66),rgba(183,20,43,.28) 48%,transparent 74%)!important}.boss-object.king-barrel{width:84px!important;height:100px!important}.boss-object.roll-barrel .object-art{animation:barrelWobble .2s ease-in-out infinite alternate!important}.boss-object.dud-barrel .boss-fuse-flame{opacity:.42}.boss-object.shell-pulse .object-art{animation:shellTreasureFloat .48s ease-in-out infinite alternate!important}
@media(max-width:520px){.boss-mechanic-banner{top:19%;font-size:.57rem;max-width:88%}.boss-object.king-barrel{width:72px!important;height:88px!important}}
`;
html=html.replace('</style>',css+'\n</style>');

const queueOld="function queueRevenge(word){if(!word)return;const key=masteryKey(word);if(!s.revenge.some(r=>r.key===key))s.revenge.push({key,wait:2})}";
const queueNew="function queueRevenge(word){if(!word)return;const key=masteryKey(word);s.lastWrongKey=key;if(!s.revenge.some(r=>r.key===key))s.revenge.push({key,wait:2})}";
if(!html.includes(queueOld))throw new Error('queueRevenge anchor missing');html=html.replace(queueOld,queueNew);

const buildBossOld="function buildBoss(){const pool=words.filter(w=>w.cat===bossTheme),target=pool[Math.floor(Math.random()*pool.length)],dec=shuffle(words.filter(w=>w.cat!==bossTheme)).slice(0,2),ans=shuffle([target,...dec]);return{type:'boss',target,answers:ans,correct:ans.indexOf(target)}}";
const buildBossNew=buildBossOld+"\nfunction buildBossForCurrent(){if(bossIndex===3&&s.lastWrongKey&&bossRound===0){const target=words.find(w=>masteryKey(w)===s.lastWrongKey);if(target){const dec=shuffle(words.filter(w=>w!==target)).slice(0,2),ans=shuffle([target,...dec]);return{type:'boss',target,answers:ans,correct:ans.indexOf(target),bossRevenge:true}}}return buildBoss()}";
if(!html.includes(buildBossOld))throw new Error('buildBoss anchor missing');html=html.replace(buildBossOld,buildBossNew);

const gameplayAnchor='function playHitFx(good,selected,correctGate){';
if(!html.includes(gameplayAnchor))throw new Error('playHitFx anchor missing');
const mechanics=String.raw`
let bossMechanicTimer=null,bossMechanicSecondaryTimer=null;
function mechanicBanner(text,ms=950){const b=$('bossMechanicBanner');if(!b)return;b.textContent=text;b.classList.add('show');clearTimeout(b._hideTimer);b._hideTimer=setTimeout(()=>b.classList.remove('show'),ms)}
function clearBossMechanicTimers(){clearTimeout(bossMechanicTimer);clearTimeout(bossMechanicSecondaryTimer);bossMechanicTimer=bossMechanicSecondaryTimer=null;$('bossMechanicBanner')?.classList.remove('show');$('arena')?.classList.remove('boss-shadow-phase')}
function swapGatePair(g,a,b){if(!g||!q||a===b||!g.children[a]||!g.children[b])return;[q.answers[a],q.answers[b]]=[q.answers[b],q.answers[a]];if(q.correct===a)q.correct=b;else if(q.correct===b)q.correct=a;const ta=g.children[a].textContent,tb=g.children[b].textContent;g.children[a].textContent=tb;g.children[b].textContent=ta;[g.children[a],g.children[b]].forEach(el=>{el.classList.add('boss-swap');setTimeout(()=>el.classList.remove('boss-swap'),560)});[...g.children].forEach((el,i)=>{el.classList.toggle('risk-gate',!!q.risk&&i===q.correct);el.classList.toggle('revenge-gate',!!q.isRevenge&&i===q.correct)})}
function shuffleAllBossGates(g){const order=shuffle([0,1,2]);const oldAnswers=[...q.answers],oldCorrect=q.correct;q.answers=order.map(i=>oldAnswers[i]);q.correct=order.indexOf(oldCorrect);[...g.children].forEach((el,i)=>{el.textContent=q.answers[i].en;el.classList.add('boss-swap');setTimeout(()=>el.classList.remove('boss-swap'),560)})}
function nudgeLane(dir,label){if(!s?.boss||!s.run)return;mechanicBanner(label,780);const target=Math.max(0,Math.min(2,s.lane+dir));if(target!==s.lane){move(target);vibrate(24)}}
function applyBossQuestionMechanic(expected,g,dur){if(!s?.boss||q!==expected)return;clearTimeout(bossMechanicTimer);clearTimeout(bossMechanicSecondaryTimer);const idx=bossIndex%BOSS_PROFILES.length,round=bossRound;
  if(idx===1&&round%2===0){mechanicBanner('⚓ Brax tauscht zwei Tore!',850);bossMechanicTimer=setTimeout(()=>{if(q===expected&&s.run)swapGatePair(g,0,2)},Math.min(1000,dur*360))}
  else if(idx===2){const wrong=[0,1,2].filter(i=>i!==q.correct);const shadow=wrong[Math.floor(Math.random()*wrong.length)];g.children[shadow]?.classList.add('shadow-gate');mechanicBanner('🌑 Blackfinn erzeugt einen Schatten!',850)}
  else if(idx===3&&q.bossRevenge){mechanicBanner('🧭 Roderick prüft dein altes Fehlerwort!',1100);$('prompt').textContent='Revanche: '+q.target.de}
  else if(idx===5){mechanicBanner('🪝 Ironhook zieht gleich an Tula!',820);[...g.children].forEach(el=>el.classList.add('boss-hook-warning'));bossMechanicTimer=setTimeout(()=>{if(q===expected&&s.run)nudgeLane(Math.random()<.5?-1:1,'🪝 HAKEN-ZUG!')},Math.min(1250,dur*430))}
  else if(idx===6){const dir=Math.random()<.5?-1:1;mechanicBanner(dir<0?'🌬 Seitenwind nach links!':'🌬 Seitenwind nach rechts!',850);[...g.children].forEach(el=>el.classList.add('boss-wind-warning'));bossMechanicTimer=setTimeout(()=>{if(q===expected&&s.run)nudgeLane(dir,'🌬 STURMBÖE!')},Math.min(1250,dur*430))}
  else if(idx===7){mechanicBanner('🗺 Corvin mischt alle Karten!',900);bossMechanicTimer=setTimeout(()=>{if(q===expected&&s.run)shuffleAllBossGates(g)},Math.min(1050,dur*370))}
  else if(idx===8){$('arena').classList.add('boss-shadow-phase');const wrong=[...g.children].filter((_,i)=>i!==q.correct);wrong.forEach(el=>el.classList.add('shadow-gate'));mechanicBanner('🌘 Azrak ruft die Schattenphase!',900)}
  else if(idx===9){if(round===0)mechanicBanner('♛ Phase 1 · Varkos entfesselt Fässer!',900);if(round===1){const dir=Math.random()<.5?-1:1;mechanicBanner('♛ Phase 2 · Königssturm!',900);bossMechanicTimer=setTimeout(()=>{if(q===expected&&s.run)nudgeLane(dir,'🌬 KÖNIGSSTURM!')},Math.min(1100,dur*390))}if(round===2){mechanicBanner('♛ Phase 3 · Tor-Tausch!',900);bossMechanicTimer=setTimeout(()=>{if(q===expected&&s.run)swapGatePair(g,0,2)},Math.min(1000,dur*360))}if(round===3){$('arena').classList.add('boss-shadow-phase');[...g.children].filter((_,i)=>i!==q.correct).forEach(el=>el.classList.add('shadow-gate'));mechanicBanner('♛ Phase 4 · Schatten!',900)}if(round>=4)mechanicBanner('♛ FINALE · Alles oder nichts!',900)}
}
function activateBossArenaMechanic(){clearBossMechanicTimers();const idx=bossIndex%BOSS_PROFILES.length;if(idx===0)mechanicBanner('💣 Pirat Kai startet den Fassregen!',1100);else if(idx===4)mechanicBanner('💰 Vargas jagt goldene Muscheln!',1100);else if(idx===9)mechanicBanner('♛ Varkos · 5 Phasen!',1100)}
function clearBossArenaMechanic(){clearBossMechanicTimers()}
function isShellKind(kind){return kind==='shell'||kind.endsWith('-shell')}
function isBarrelKind(kind){return kind==='barrel'||kind.endsWith('-barrel')}
function chooseBossObjectKind(){const idx=bossIndex%BOSS_PROFILES.length,r=Math.random();if(idx===0)return r<.72?(r<.18?'roll-barrel':r<.34?'double-barrel':'barrel'):(r<.9?'shell':'gold-shell');if(idx===4)return r<.48?(r<.18?'thief-shell':r<.3?'gold-shell':'shell'):(r<.68?'barrel':r<.78?'shield-shell':r<.88?'time-shell':'combo-shell');if(idx===9)return r<.68?(r<.2?'king-barrel':r<.36?'roll-barrel':'barrel'):(r<.82?'gold-shell':r<.91?'shield-shell':'time-shell');if(r<.54)return r<.12?'roll-barrel':r<.2?'double-barrel':r<.27?'dud-barrel':'barrel';if(r<.68)return'gold-shell';if(r<.79)return'shield-shell';if(r<.9)return'time-shell';if(r<.97)return'combo-shell';return'shell'}
function collectBossTreasure(el){if(!el||el.dataset.resolved)return;el.dataset.resolved='1';el.classList.add('collected');bossBurst(el,true);const kind=el.dataset.kind||'shell';let shellGain=1,points=75,xp=2,msg='🐚 Muschel eingesammelt!';if(kind==='gold-shell'){shellGain=3;points=180;msg='✨ Goldmuschel! +3'}else if(kind==='thief-shell'){shellGain=2;points=160;msg='💰 Vargas-Muschel gerettet! +2'}else if(kind==='shield-shell'){s.shieldCharges=(s.shieldCharges||0)+1;points=110;msg='🛡 Schutzmuschel geladen!'}else if(kind==='time-shell'){s.timeBoostUntil=Date.now()+8000;points=100;msg='⏳ Zeitmuschel · Tore werden langsamer!'}else if(kind==='combo-shell'){s.comboShield=(s.comboShield||0)+1;points=100;msg='🔗 Combo-Muschel geladen!'}if(perkActive('muschelmagnet')&&shellGain>0)shellGain++;if(perkActive('doppelter-schatz')&&shellGain>0)shellGain+=2;s.shells+=shellGain;s.score+=points;s.xp+=xp;render();feedback(msg+' +'+points);vibrate(24);setTimeout(()=>el.remove(),460)}
function explodeBossHazard(el,hit){if(!el||el.dataset.resolved)return;el.dataset.resolved='1';const kind=el.dataset.kind||'barrel';if(kind==='dud-barrel'){el.classList.add('exploding');feedback('💨 Blindgänger! Glück gehabt.');vibrate(16);setTimeout(()=>el.remove(),480);return}el.classList.add('exploding');const ring=document.createElement('span');ring.className='boss-explosion-ring';el.appendChild(ring);bossBurst(el,false);if(kind==='double-barrel'){setTimeout(()=>{if(el.isConnected){const ring2=document.createElement('span');ring2.className='boss-explosion-ring';el.appendChild(ring2);bossBurst(el,false)}},170)}if(hit&&s?.boss){if((s.shieldCharges||0)>0){s.shieldCharges--;feedback('🛡 Schutzmuschel blockt das Fass!');vibrate(30)}else if(perkActive('schutzschild')&&!s.perkShieldUsed){s.perkShieldUsed=true;feedback('🛡 Tulas Schutzschild hält!');vibrate(30)}else{s.bossMiss=Math.min(3,s.bossMiss+1);if(perkActive('kombokette'))s.combo=Math.floor(s.combo/2);else s.combo=0;render();feedback(kind==='king-barrel'?'💥 KÖNIGSFASS! Boss-Versuch verloren':'💥 Fass! Boss-Versuch verloren');playCharacterReactions(false);vibrate([62,34,76]);if(s.bossMiss>=3)stopBossObjects(false)}}setTimeout(()=>el.remove(),620)}
`;
html=html.replace(gameplayAnchor,mechanics+'\n'+gameplayAnchor);

const oldCollect="function collectBossShell(el){if(!el||el.dataset.resolved)return;el.dataset.resolved='1';el.classList.add('collected');bossBurst(el,true);s.shells+=1;s.score+=75;s.xp+=2;render();feedback('🐚 Muschel eingesammelt! +75');vibrate(24);setTimeout(()=>el.remove(),460)}";
const oldExplode="function explodeBossBarrel(el,hit){if(!el||el.dataset.resolved)return;el.dataset.resolved='1';el.classList.add('exploding');const ring=document.createElement('span');ring.className='boss-explosion-ring';el.appendChild(ring);bossBurst(el,false);if(hit&&s?.boss){s.bossMiss=Math.min(3,s.bossMiss+1);s.combo=0;render();feedback('💥 Explosives Fass! Boss-Versuch verloren');playCharacterReactions(false);vibrate([62,34,76]);if(s.bossMiss>=3)stopBossObjects(false)}setTimeout(()=>el.remove(),560)}";
if(!html.includes(oldCollect)||!html.includes(oldExplode))throw new Error('Boss object handlers missing');html=html.replace(oldCollect,'function collectBossShell(el){return collectBossTreasure(el)}').replace(oldExplode,'function explodeBossBarrel(el,hit){return explodeBossHazard(el,hit)}');

const spawnObjOld="function spawnBossObject(){if(!s?.boss||!s.run||s.paused||s.bossResult||s.bossMiss>=3)return;const layer=ensureBossObjectLayer();if(!layer||layer.children.length>=4)return;const kind=Math.random()<.58?'barrel':'shell',lane=Math.floor(Math.random()*3),id=++bossObjectCounter,el=document.createElement('div');el.className='boss-object '+kind;el.dataset.kind=kind;el.dataset.lane=String(lane);el.style.setProperty('--lane-left',laneLeft[lane]);el.style.setProperty('--fall-time',(kind==='barrel'?3.55+Math.random()*.7:3.9+Math.random()*.9).toFixed(2)+'s');el.innerHTML='<div class=\"object-art\">'+(kind==='barrel'?createBarrelSvg(id):createShellSvg(id))+'</div>';el.addEventListener('animationend',event=>{if(event.animationName!=='bossObjectFall'||el.dataset.resolved)return;if(kind==='barrel')explodeBossBarrel(el,false);else el.remove()},{once:true});layer.appendChild(el)}";
const spawnObjNew="function spawnBossObject(){if(!s?.boss||!s.run||s.paused||s.bossResult||s.bossMiss>=3)return;const layer=ensureBossObjectLayer();if(!layer||layer.children.length>=5)return;const kind=chooseBossObjectKind(),lane=Math.floor(Math.random()*3),id=++bossObjectCounter,el=document.createElement('div');el.className='boss-object '+kind+(isShellKind(kind)?' shell-pulse':'');el.dataset.kind=kind;el.dataset.lane=String(lane);el.style.setProperty('--lane-left',laneLeft[lane]);el.style.setProperty('--fall-time',(isBarrelKind(kind)?3.45+Math.random()*.72:3.75+Math.random()*.92).toFixed(2)+'s');el.innerHTML='<div class=\"object-art\">'+(isBarrelKind(kind)?createBarrelSvg(id):createShellSvg(id))+'</div>';if(kind==='roll-barrel')setTimeout(()=>{if(el.isConnected&&!el.dataset.resolved){const next=Math.max(0,Math.min(2,lane+(Math.random()<.5?-1:1)));el.dataset.lane=String(next);el.style.setProperty('--lane-left',laneLeft[next])}},1350);el.addEventListener('animationend',event=>{if(event.animationName!=='bossObjectFall'||el.dataset.resolved)return;if(isBarrelKind(kind))explodeBossHazard(el,false);else{if(kind==='thief-shell')mechanicBanner('💰 Vargas schnappt die Muschel!',780);el.remove()}},{once:true});layer.appendChild(el)}";
if(!html.includes(spawnObjOld))throw new Error('spawnBossObject anchor missing');html=html.replace(spawnObjOld,spawnObjNew);

const collisionOld="function checkBossObjectCollisions(){if(!s?.boss||!s.run||s.paused||s.bossResult)return;const runner=$('runner');if(!runner)return;const rr=runner.getBoundingClientRect();ensureBossObjectLayer()?.querySelectorAll('.boss-object:not([data-resolved])').forEach(el=>{const r=el.getBoundingClientRect();if(r.top<rr.top-30||r.bottom>rr.bottom+34)return;if(!bossRectsOverlap(r,rr))return;if(el.dataset.kind==='shell')collectBossShell(el);else explodeBossBarrel(el,true)})}";
const collisionNew="function checkBossObjectCollisions(){if(!s?.boss||!s.run||s.paused||s.bossResult)return;const runner=$('runner');if(!runner)return;const rr=runner.getBoundingClientRect(),magnet=perkActive('muschelmagnet')?52:0;ensureBossObjectLayer()?.querySelectorAll('.boss-object:not([data-resolved])').forEach(el=>{let r=el.getBoundingClientRect();if(r.top<rr.top-36||r.bottom>rr.bottom+38)return;if(isShellKind(el.dataset.kind)&&magnet){r={left:r.left-magnet,right:r.right+magnet,top:r.top,bottom:r.bottom,width:r.width+magnet*2,height:r.height}}if(!bossRectsOverlap(r,rr))return;if(isShellKind(el.dataset.kind))collectBossTreasure(el);else explodeBossHazard(el,true)})}";
if(!html.includes(collisionOld))throw new Error('Collision anchor missing');html=html.replace(collisionOld,collisionNew);

const startObjectsOld="function startBossObjects(){stopBossObjects();const layer=ensureBossObjectLayer();if(!layer)return;layer.innerHTML='';bossObjectInitialTimer=setTimeout(spawnBossObject,520);bossObjectSpawnTimer=setInterval(spawnBossObject,1320);bossObjectCollisionTimer=setInterval(checkBossObjectCollisions,65)}";
const startObjectsNew="function startBossObjects(){stopBossObjects();const layer=ensureBossObjectLayer();if(!layer)return;layer.innerHTML='';const interval=bossIndex===0?900:bossIndex===9?980:bossIndex===4?1080:1260;bossObjectInitialTimer=setTimeout(spawnBossObject,480);bossObjectSpawnTimer=setInterval(spawnBossObject,interval);bossObjectCollisionTimer=setInterval(checkBossObjectCollisions,60)}";
if(!html.includes(startObjectsOld))throw new Error('startBossObjects anchor missing');html=html.replace(startObjectsOld,startObjectsNew);

const spawnMeta="q=s.boss?buildBoss():buildNormal();";
if(!html.includes(spawnMeta))throw new Error('Meta spawn q anchor missing');html=html.replace(spawnMeta,"q=s.boss?buildBossForCurrent():buildNormal();");
const spawnTail="if(perkActive('piratenblick')&&!s.boss)setTimeout(()=>{if(q===expected&&s.run){const wrong=[...g.children].filter((_,i)=>i!==q.correct);wrong[0]?.classList.add('wrong')}},Math.min(1250,dur*480));timer=setTimeout(resolve,dur*1000+80)";
if(!html.includes(spawnTail))throw new Error('Meta spawn tail missing');html=html.replace(spawnTail,"if(perkActive('piratenblick')&&!s.boss)setTimeout(()=>{if(q===expected&&s.run){const wrong=[...g.children].filter((_,i)=>i!==q.correct);wrong[0]?.classList.add('wrong')}},Math.min(1250,dur*480));if(s.boss)applyBossQuestionMechanic(expected,g,dur);timer=setTimeout(resolve,dur*1000+80)");

const startBossOld="function startBoss(){const boss=currentBoss();BOSS_IMG=boss.image;$('bossSideImg').src=boss.image;$('bossSideImg').alt=boss.name;s.boss=true;s.bossCorrect=0;s.bossMiss=0;bossRound=0;s.run=true;$('bossIntro').classList.add('hidden');$('bossSide').classList.add('show');$('bossSide').classList.toggle('final-boss',bossIndex===BOSS_PROFILES.length-1);render();spawn();startBossObjects()}";
const startBossNew="function startBoss(){const boss=currentBoss();BOSS_IMG=boss.image;$('bossSideImg').src=boss.image;$('bossSideImg').alt=boss.name;s.boss=true;s.bossCorrect=0;s.bossMiss=0;s.perkShieldUsed=false;bossRound=0;s.run=true;$('bossIntro').classList.add('hidden');$('bossSide').classList.add('show');$('bossSide').classList.toggle('final-boss',bossIndex===BOSS_PROFILES.length-1);activateBossArenaMechanic();render();spawn();startBossObjects()}";
if(!html.includes(startBossOld))throw new Error('startBoss anchor missing');html=html.replace(startBossOld,startBossNew);

const resultStart="function showBossResult(won){clearTimeout(timer);const boss=currentBoss();s.run=false;";
if(!html.includes(resultStart))throw new Error('showBossResult anchor missing');html=html.replace(resultStart,"function showBossResult(won){clearBossArenaMechanic();clearTimeout(timer);const boss=currentBoss();s.run=false;");

await fs.writeFile(path,html,'utf8');
console.log('Applied ten unique boss mechanics and expanded shell/barrel variants.');
